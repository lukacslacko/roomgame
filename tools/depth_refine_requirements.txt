# Optional dependencies for the Depth Anything V2 depth refiner
# (tools/depth_refine.py). Only needed if you actually want to refine
# captured sessions; the rest of the pipeline runs without these.
#
#   pip install -r tools/depth_refine_requirements.txt
#
# torch+torchvision pulls ~1 GB; the model weights add another ~1.3 GB on
# first run (cached under ~/.cache/huggingface). On Apple Silicon, MPS is
# auto-detected and used by default — no extra setup required.

torch>=2.5
torchvision>=0.20
transformers>=4.40   # depth-estimation pipeline + DPT/DepthAnythingV2 support
pillow>=10.0         # PIL.Image.fromarray on the captured colour buffers
